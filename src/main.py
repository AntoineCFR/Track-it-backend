import os
from firestore_API.firestore_helpers import firestore_main

def main():
    #firestore_secret = os.environ.get('FIRESTORE_SECRET')
    #firestore_main(firestore_secret)
    return 1

if __name__ == "__main__":
    main()